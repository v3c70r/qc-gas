{
  "name": "essence-quebec",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:py": "python3 -m unittest discover -s scripts -p 'test_*.py'",
    "test:all": "npm run test:py && npm test",
    "test:install": "playwright install chromium",
    "icons": "node scripts/create_pwa_icons.mjs",
    "agent:watch": "node .agent/pipeline.mjs watch",
    "agent:status": "node .agent/pipeline.mjs status"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "mitsupi": "^1.6.0"
  }
}
